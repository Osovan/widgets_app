import 'package:flutter/material.dart';

class Animatedscreen extends StatelessWidget {
  static const name = 'animacted_screen';
  const Animatedscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
